26. remove duplicates from sorted array
class Solution {
public:
    int removeDuplicates(vector<int>& nums) {
        int flag = 1;
        for (int i = 1; i < nums.size(); i++) {
            int number = nums[i];
            if (nums[i] != nums[i - 1]) {
            nums[flag] = nums[i];
            flag++;       
            }
    }
    return flag;

    }
};
